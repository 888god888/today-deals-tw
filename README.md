# 今天有好康

每天彙整台灣 3C、遊戲票券、免費任務、餐飲食品、家電日用、男性運動與金融支付優惠的靜態網站。

## 已完成

- 手機與桌面響應式介面
- 七類興趣白名單、多標籤分類、關鍵字、官方來源與排序篩選
- 瀏覽器本機收藏
- PTT 省錢板，以及麥當勞、肯德基、全家、萊爾富、LINE Pay、PChome 24h、Yahoo 購物中心與蝦皮官方優惠抓取器
- 即時優惠雷達：以近期新聞主動尋找「下殺、破盤、限時特價」，補足固定活動首頁看不到的突發消息
- 全網強折扣雷達：每天動態搜尋 Threads、Dcard、Mobile01、購物平台與一般網頁，不預設品牌；今天出現什麼大折扣就收什麼
- 進入活動內頁整理「拿到什麼、適用條件、領取步驟、限制與優惠碼」
- 預設排除女性專用品與交通資訊；購物來源只保留指定興趣品類
- 強折扣門檻：只保留免費、買一送一、7.5 折以下、至少 15% 回饋、現折 1,000 元以上、可計算出降幅至少 25%，或有數字佐證的歷史低價／破盤價
- 單一來源抓取失敗時保留上一版資料
- GitHub Actions 每天台灣時間 08:15、18:15 更新並部署 GitHub Pages
- 過期活動自動隱藏，資料頁保留來源狀態

## 第一次部署

1. 在 GitHub 建立一個空白 repository。
2. 把本專案所有檔案上傳到 `main` 分支。
3. 到 repository 的 **Settings → Pages → Build and deployment**。
4. 將 **Source** 選成 **GitHub Actions**。
5. 到 **Actions** 開啟「更新每日好康並部署」，按 **Run workflow**。
6. 執行完成後，網址會顯示在該次 workflow 的 `deploy` 結果與 Settings → Pages。

## 調整來源

編輯 `config/sources.json` 可以停用來源、調整抓取頁數、筆數，或修改 `web_discovery.queries` 的動態搜尋範圍；不需要維護品牌清單。各站改版可能導致解析失敗；頁面下方會顯示來源狀態，失敗時不會清空前一次成功資料。

Threads 沒有適合這種免登入靜態專案使用的穩定公開搜尋介面，直接爬取也很容易被限制。因此本版改從公開搜尋索引抓 Threads 貼文，再與新聞、論壇及購物活動頁混合整理。來源暫時無法讀取時，會保留前一次成功資料。

## 本機查看

請在專案目錄執行：

```bash
python -m http.server 8000 -d dist
```

再開啟 `http://localhost:8000`。不要直接雙擊 `index.html`，因為瀏覽器可能阻擋它讀取 JSON。

## 資料提醒

本網站只提供活動索引與簡短摘要，不取代主辦單位公告。自動抓取僅讀取公開頁面，應控制頻率並遵守來源網站規範；若某來源要求停止自動存取，請在設定中將它停用。
